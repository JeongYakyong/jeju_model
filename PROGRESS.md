# PROGRESS

> 스냅샷 (일지 아님). 상세 로그는 `jejumodel.md`, 결정 목록은 `DECISIONS.md`.
> 최종 갱신 2026-08-21

## 완료된 것

- **Phase 1~3 완료** — KIMR 은 std NC 단일 경로(`kma_kimr_grib.py` 삭제), 서빙 입력 met 도 NC.
- **수집 사다리** — ①KIMG → ②시간 보간(연속 2개) → ③KIMR 대체 → ④sentinel 정정·clip·건전성 검사.
  순서가 곧 안전장치다. 9999 는 결측으로 정정(`cape` 평균 2073.0 → 256.6).
- **KIMG 13 → 22변수 + 180일 백필** (`forecast_kimg` 237 base). 건전성 감사 통과.
- **태양광 일 스케일링 재설계** — 과적합 실증 후 자유 파라미터 5 → 2 (mid 0.45 / k 3.5 / floor 0.10).
- ★**시각별 편향 보정은 종결됐다 — 도입 안 함** (사용자 결정 2026-08-21, 재질문 금지).
  이득은 실재하나(홀드아웃 −17~22%) robust·솔직 우선. **08-12 의 "보정 후 KIMR 이 KIMG 를
  이긴다"는 재현 실패로 철회**됐다 — 보정과 무관하게 기온·습도는 KIMG, 풍속은 KIMR 우세.
- **KPX `*_da` 중복 버그 수정** — API 가 1~9시를 `'01'`/`'1'` 두 형식으로 발행해 하루 66행.
  안 걸러내면 `build_historical` 이 `InvalidIndexError` 로 죽는다.

## 현재 상태

`historical` 58,195행 / 최신 2026-08-21 18:00. `forecast_horizon` 224 base(백필 진행 중).
서버 offline 이라 파이프라인은 2026-08-04 이후 미실행(의도된 상태).

- ⚠ `*_da` 08-08~08-21 **결손** — KPX 일일 요청제한 소진. 자정 리셋 후 재실행하면 채워진다.
- ⚠ 예보 백필 실행 중 — base 당 **약 6분**(12z 5일), 18개에 2시간 안팎. resume-skip 이라 안전.

## 다음 할 일

1. **자정 후** `python collectors/collect_historical.py --backfill 20` — `*_da` 채우기.
2. ② 끝나면 **②-2** `collect_archive --backfill 23` (`forecast_kimr` 23 / `kimg` 10 base 결손).
3. **화면 통보** — `src_solar_cloud='KIMR'` 배지가 없다(`pages/common.py`).
4. **서빙 체인 소스 구조 결정** — 더 기다릴 이유 없다. 기온·습도 KIMG / 풍속 KIMR.
5. **8월 스케일링 재판정** — `fit_solar_scale.py --check` 로 흐림 편향 **부호** 확인.
6. **git push** (사용자). 로컬 커밋이 리모트 `Initial commit` 위에 쌓여 있다.
7. **2026-12**: NC `GRAUPEL` = 구 `TCOG` 값 일치 확인(겨울에만 `tcog>0`).

### 닫힌 길 (다시 열지 말 것)
- **시각별 편향 보정** (2026-08-21 사용자 결정 — 성능이 아니라 방침 문제다).
- **일사 결합(KIMG+누적 ACSWDNB)** — 망가진 KIMG 를 보정하던 착시. 재수집 후 이득 소멸.
- **CAPE·CINN·HPBL·MSLP 를 태양광 신호로** — 잔차 상관 |r| ≤ 0.055.
- **일사·운량 소스 폴백을 시간 보간보다 먼저** — 정답 대조에서 MAE 5~7배 나쁨.
- **SMP D+2 안정 구간 블렌딩** / **시점별 일사 QM** / **전면 KIMR 전환**.

## 주의사항

- ★**입력 지표 개선이 출력 개선으로 자동 전달되지 않는다.** 입력을 손대면 서빙 A/B 로 출력까지 재라.
- ★**자유도를 늘리면 과적합한다.** 후처리 파라미터는 **완전 홀드아웃**으로 재고, 검증창을 선택에 쓰지 말 것.
- ★**"값이 틀린" 결손은 NULL 검사로 못 잡는다** — 소스 대조나 `sanity_check` 물리 모순 검사가 유일한 방어.
- **KPX 쿼터는 서비스별이다** — 백필 전 진단 호출을 아낄 것. 소진되면 그날은 끝이다.
- **`forecast_horizon` 의 옛 `cape`/`cinn` 은 재학습에 쓰면 안 된다** — `src_met_proto` 로 거를 것.
- **스케일링은 낮추기만 한다** — 예보가 과소예측하는 달엔 손해. 파라미터로 못 고친다.
- **PatchTST 가중치 D+1~D+5 는 스케일러 공유 = 한 세트.** 일부만 반입하면 조용히 틀린다.
- `patchtst_signal` 은 2026-05-31 에서 축적이 멈췄다(학습 전용, 서빙 영향 없음).
