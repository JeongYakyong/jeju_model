# PROGRESS

> 스냅샷 (일지 아님). 상세 로그는 `jejumodel.md`, 결정 목록은 `DECISIONS.md`.
> 최종 갱신 2026-08-12

## 완료된 것

- **Phase 1~3 완료. KIMR 은 std NC 단일 경로다** (`kma_kimr_grib.py` 삭제).
  서빙 입력 met 도 NC 로 전환했고 **본 DB 실전 수집까지 확인**했다
  (met r ≥ 0.99998 / 일사·운량 완전 일치 / 서빙 A/B 수요 차이 0.002% / 9999 sentinel 0개).
- **수집 사다리 완성** — 결손·이상치·수집불가 세 축에 안전장치를 넣었다.
  ①KIMG → ②시간 보간(연속 2개) → ③KIMR 대체 → ④sentinel 정정·clip·건전성 검사.
  순서가 곧 안전장치다(상세는 `CLAUDE.md`).
- **9999 는 결측으로 정정** — `cape` 평균이 2073.0 → 256.6 으로 바뀔 만큼 통계를
  오염시키고 있었다. `drop_sentinels` 가 `clip_ranges` 앞에서 NaN 으로 바꾼다.
- **이상치 탐지** (`sanity_check`) — 결측률·얼어붙음·급변·물리 모순.
  합성 손상 6종 전부 탐지, 정상 base 오탐 0. rc=1 로 파이프라인에 신호를 준다.
- **KIMG 수집 13 → 22변수** (한 콜에 전부 오므로 호출수 증가 0) + **180일 백필 완료**
  (`forecast_kimg` 33,660행 / 237 base, met 185 base / 확장변수 173 base).
- **`--verify` 신뢰 회복** — 기대 행수를 창에서 계산, NULL 검사를 운영 지평으로 제한,
  sentinel 에 `total_cloud_*` 추가. 현재 224 base 전부 완전.
- **태양광 일 스케일링 재설계** — 과적합 실증 후 자유 파라미터 5 → 2.
  배포 mid 0.45 / k 3.5 / floor 0.10(지평 공통), 지표 `radiation_south` 단독 P60.
- **SMP D+2 원인분석** — 개선안 기각(안정 구간을 사전에 식별할 수단이 없다).
- **git 연결** — 리모트 `JeongYakyong/jeju_model.git`, 커밋 7개. **push 는 미실행.**

## 현재 상태

`serve_chain --utc 12 --no-write` = 120행 hd 1~5, 수요 931MW / net_load 694MW.
selftest_pivot **21항목** / `--verify` 224 base 전부 완전 / AppTest exception 0.
`forecast_horizon` 32,152행 224 base · `forecast_kimg` 33,660행 237 base.

- **파이프라인 단계 구성(`PIPELINE_STEPS`)은 안 바꿨다** — 바뀐 건 각 단계 **내부** 동작뿐.
- 서버가 offline 이라 파이프라인이 2026-08-04 이후 안 돌았다(의도된 상태).
- **KIMG(메인) / KIMR(서브)** — 서빙 일사·운량은 KIMG, met 은 KIMR 우선(combine_first).

## 다음 할 일

1. **화면 통보** — `src_solar_cloud='KIMR'` 이 DB 에는 남지만 화면 배지가 없다.
   대체 소스로 만든 예측임을 사용자가 볼 수 있어야 한다(`pages/common.py`).
2. **서빙 체인 소스 구조 결정** — 이제 KIMG met 이 185 base 쌓여 비교가 가능하다.
   단, **소스를 바꾸기 전에 시각별 편향 보정을 먼저 보라**(아래 3).
3. ★**시각별 편향 보정** — 이번 세션 최대 발견. NWP 가 일변화 진폭을 크게 눌러
   재현한다(실측 3.36°C vs KIMG 1.28 / KIMR 0.84). 편향이 **지평에 안정**해서
   보정이 쉽고, 시각 보정만으로 KIMR 기온 MAE 가 **−13.5%** 개선된다.
   보정 후엔 KIMR 이 KIMG 를 이기므로 **소스 교체보다 지렛대가 크다.**
   ⚠ 계절엔 불안정하니 고정 보정표는 드리프트에 취약. 서빙 A/B 필수.
4. **8월 스케일링 재판정** — `fit_solar_scale.py --check` 로 흐림 편향 **부호** 확인.
5. **git push** (사용자). 로컬 커밋 7개가 리모트 `Initial commit` 위에 있다.
6. **2026-12**: NC `GRAUPEL` = 구 `TCOG` 값 일치 확인(겨울에만 `tcog>0`).

### 닫힌 길 (다시 열지 말 것)
- **일사 결합(KIMG+누적 ACSWDNB)** — "7~19% 개선"은 망가진 KIMG 를 보정하던 착시.
  재수집 후 이득 소멸(0.6021 vs 0.6022), 서빙 전달률 −0.31%.
- **CAPE·CINN·HPBL·MSLP 를 태양광 신호로** — 잔차 상관 |r| ≤ 0.055. 산란비도 열세.
- **일사·운량 소스 폴백을 시간 보간보다 먼저** — 정답 대조에서 MAE 5~7배 나쁨.
- **SMP D+2 안정 구간 shrinkage/블렌딩** — 그 구간을 사전에 식별할 수 없다.
- **시점별 일사 QM** / **전면 KIMR 전환** — 각각 2026-07-31 실측으로 기각.

## 주의사항

- ★**입력 지표 개선이 출력 개선으로 자동 전달되지 않는다** — 특히 개선분이 **분산 축소**
  에서 올 때(일사 결합이 그랬다: std 를 실측의 0.792배로 압축). 입력을 손대면 반드시
  **서빙 A/B 로 출력까지** 재라.
- ★**자유도를 늘리면 과적합한다** — 지표 전탐색·k 격자 탐색에서 두 번 확인.
  후처리 파라미터는 **완전 홀드아웃**으로 재고, 검증창을 선택에 쓰지 말 것.
- ★**"값이 틀린" 결손은 NULL 검사로 못 잡는다** — 2026-07 일사가 그랬다(그럴듯한
  틀린 값). 소스 간 대조나 `sanity_check` 의 물리 모순 검사가 유일한 방어다.
- **`forecast_horizon` 의 옛 `cape`/`cinn` 은 재학습에 쓰면 안 된다** — GRIB 시절
  9999 가 57%/68%, 2바이트 랩어라운드(655.36 배수)까지. `src_met_proto` 로 거를 것.
- **스케일링은 낮추기만 한다** — 예보가 과소예측하는 달엔 손해. 파라미터로 못 고친다.
- `--check` 는 `est_horizon_jeju`(그때그때 cron 산출)를 읽는다 — **배포일 이전 구간은
  옛 파라미터로 만든 값**이라 배포 직후 수치를 "지금 성능"으로 읽으면 안 된다.
- `patchtst_signal` 은 2026-05-31 에서 축적이 멈췄다. **학습 전용**이라 서빙 영향 없음.
- PatchTST 가중치는 D+1~D+5 가 **한 세트**(스케일러 공유). `models/` 가 git 에 들어가므로
  재학습마다 수십 MB 누적 — 잦아지면 Git LFS 로.
- `Training/**/no use/` 는 커밋 `aee9bab` 에서 복구 가능(9MB, 삭제 전 보관).
