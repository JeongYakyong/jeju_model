# PROGRESS

> 스냅샷 (일지 아님). 상세 로그는 `jejumodel.md`, 결정 목록은 `DECISIONS.md`.
> 최종 갱신 2026-08-04

## 완료된 것

- **Phase 1~3 전부 완료.** KIMR 은 std NC 단일 경로다 (`kma_kimr_grib.py` 삭제).
  서빙 입력 met 도 NC 로 전환했고 **본 DB 실전 수집까지 확인**했다.
  검증: met r ≥ 0.99998 / 일사·운량 완전 일치 / 서빙 A/B 수요 차이 0.002% / 9999 sentinel 0개.
- **3h 결손 보간을 수집 단계로** — `postprocess.fill_short_gaps`(limit=2, 외삽 금지)를
  `collect_forecast`·`collect_archive` 가 공용. `clip_ranges` **앞**에 둔다.
- **2026-07-02~11 결손 base 10개 재수집** — 이번 세션 최대 수확이었다.
  운량 56/134 → 120/120, **일사 실측 대비 MAE 1.0786 → 0.6432(−40%)**.
  운량은 NULL 이라 보였지만 **일사는 값이 틀린 채 채워져 있어** 아무 검사에도 안 걸렸다.
- **`--verify` 신뢰 회복** — 기대 행수를 창에서 계산(구 하드코딩 144 는 D+7 시절),
  NULL 검사를 운영 지평으로 제한, **sentinel 에 `total_cloud_*` 추가**(이번 사고를 잡는다).
  현재 224 base 전부 완전.
- **태양광 일 스케일링 재설계 + 수정 데이터로 재적합** — 자유 파라미터 5개 → 2개.
  배포 mid 0.45 / k 3.5 / floor 0.10(지평 공통), 지표 `radiation_south` 단독 P60.
- **SMP D+2 원인분석** — 개선안 기각(사전 식별 불가).
- **git 연결** — 프로젝트 루트에 리모트(`JeongYakyong/jeju_model.git`)·이력 유지. **push 는 미실행.**
- `Training/**/no use/` 삭제 (커밋 `aee9bab` 에서 복구 가능). Training 14M → 5.9M.

## 현재 상태

`serve_chain --utc 12 --no-write` = 120행 hd 1~5, 수요 931MW / net_load 694MW (base 08-03).
`--verify` 224 base 전부 완전 / selftest_pivot 17항목 / AppTest exception 0.
`forecast_horizon` 32,152행 224 base · `est_horizon_jeju` 37,584행 224 base.

- **병목은 여전히 예보다** — D+3 부터 예보 오차 > 모델 오차.
- **KIMG(메인) / KIMR(서브)** — 서빙 일사·운량은 KIMG, met 은 KIMR 우선(combine_first).

## 다음 할 일

1. **KIMG met 을 east·west·south 제대로 수집** (사용자 진행 예정).
   지금 `forecast_kimg` 의 met 은 **12 base(1,388행)뿐**이라 소스 비교가 불가능하다
   ("KIMG met 은 설계상 안 쓴다"로 수집을 안 했다).
   → 쌓이면 **서빙 체인 소스 구조**를 정한다. 사용자 제안: ①KIMG 단독 ②결측 시 KIMR 대체
     ③KIMG 수집 실패 시 KIMR 전체. 구조는 타당하나 **어느 변수를 어느 소스로 할지**는
     실측으로 정해야 한다 — 12 base 예비 측정은 **엇갈렸다**:
     기온 KIMG 우세(0.828 vs 0.878) / **풍속 KIMR 우세(1.674 vs 1.928)** / 습도 KIMG 우세(3.45 vs 5.05).
     표본이 얇고(7/08~7/22 단일 창) 계절도 하나뿐이라 이걸로 결정하면 안 된다.
   - ★현재 **일사·운량에는 폴백이 아예 없다** — KIMG 가 빠지면 NULL 로 남는다(이번 사고가
     정확히 그것). `forecast_kimr` 에 CLDFRA 운량·일사가 있으니 폴백 배선은 가능하다.
     사용자 제안 ②③은 이 구멍을 메우는 것이라 **소스 우선순위와 무관하게 가치가 있다.**
2. **8월 데이터로 스케일링 재판정** — 7월 흐림 편향이 음수(−0.049)로 뒤집힌 게 계절인지
   표본 노이즈(흐림날 8일)인지 아직 모른다. `fit_solar_scale.py --check` 로 부호 확인.
3. **git push** (사용자). 로컬 커밋 5개가 리모트의 `Initial commit` 위에 쌓여 있다.
4. **2026-12**: NC `GRAUPEL` = 구 `TCOG` 값 일치 확인 (겨울에만 `tcog>0` 이라 지금은 불가).

### 닫힌 길 (다시 열지 말 것)
- **일사 결합(KIMG+누적 ACSWDNB)** — "7~19% 개선"은 망가진 KIMG 를 보정하던 착시였다.
  재수집 후 이득 소멸(KIMG 0.6021 vs 결합 0.6022), 서빙 전달률 −0.31%(흐림은 +1.28% 악화).
- **CAPE·CINN·HPBL·MSLP** — 태양광 잔차 상관 |r| ≤ 0.055. 신호 없음. 산란비도 열세.
- **SMP D+2 안정 구간 shrinkage/블렌딩** — 그 구간을 사전에 식별할 수단이 없다.
- **시점별 일사 QM** — 조건부 편향을 못 잡는다(2026-07-31).
- **전면 KIMR 전환(일사·운량 포함)** — 2026-07-31 실측으로 기각.

## 주의사항

- ★**입력 지표 개선이 출력 개선으로 자동 전달되지 않는다** — 특히 개선분이 **분산 축소**에서
  올 때. 일사 결합이 그랬다(무제약 최소제곱은 std 를 실측의 0.792배로 압축). 입력을 손대면
  반드시 **서빙 A/B 로 출력까지** 재라.
- ★**자유도를 늘리면 과적합한다** — 이번에 두 번 확인(지표 전탐색, k 격자 탐색).
  후처리 파라미터는 **완전 홀드아웃**으로 재고, 검증창을 선택에 쓰지 말 것.
- ★**"값이 틀린" 결손은 어떤 검사에도 안 걸린다** — NULL 은 sentinel 이 잡지만
  2026-07 일사처럼 *그럴듯한 틀린 값*은 못 잡는다. 소스 간 대조가 유일한 방어다.
- **`forecast_horizon` 의 옛 `cape`/`cinn` 은 재학습에 쓰면 안 된다** — GRIB 시절
  9999 sentinel 이 57%/68%, 2바이트 랩어라운드(655.36 배수)까지. `src_met_proto` 로 거를 것.
- **스케일링은 낮추기만 한다** — 예보가 과소예측하는 달엔 손해. 파라미터로 못 고친다.
- `--check` 는 `est_horizon_jeju`(그때그때 cron 산출)를 읽는다 — **배포일 이전 구간은 옛
  파라미터로 만든 값**이라 배포 직후 수치를 "지금 성능"으로 읽으면 안 된다.
- `patchtst_signal` 은 2026-05-31 에서 축적이 멈췄다. **학습 전용**이라 서빙·화면 영향 없음.
- PatchTST 가중치는 D+1~D+5 가 **한 세트**(스케일러 공유). `models/` 가 git 에 들어가므로
  재학습마다 수십 MB 누적 — 잦아지면 Git LFS 로.
- 백업(스크래치): `backup_grib_20260804/`, `solar_scale_BEFORE_20260804.json`,
  `solar_scale_BEFORE_recollect.json`, `input_data_jeju_BEFORE_nc_run.db`.
