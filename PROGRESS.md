# PROGRESS

> 스냅샷 (일지 아님). 상세 로그는 `jejumodel.md`, 결정 목록은 `DECISIONS.md`.
> 최종 갱신 2026-08-24

## 완료된 것

- **Phase 1~3 완료** — KIMR 은 std NC 단일 경로, 서빙 입력 met 도 NC.
- **수집 사다리** — ①KIMG → ②시간 보간(연속 2개) → ③KIMR 대체 → ④sentinel 정정·clip·건전성 검사.
- **KIMG 13 → 22변수 + 180일 백필**. **태양광 일 스케일링 자유도 5 → 2**.
- ★**시각별 편향 보정은 종결 — 도입 안 함** (사용자 결정 2026-08-21, 재질문 금지).
  robust·솔직 우선. 08-12 의 "보정 후 KIMR 이 KIMG 를 이긴다"는 재현 실패로 철회.
- ★**서빙 met 소스 구조 현행 유지로 확정** (2026-08-24) — `combine_first`(KIMR 우선) 그대로.
  기온·습도 KIMG 전환은 **모델 입력 형태(3지점 평균)로 재면 월별 4승 3패라 노이즈**.
  풍속 KIMR 우위(8.8~13.6%, 월 7/7·6/7)는 진짜이나 **이미 KIMR**.
- ★**west 좌표 의혹 종결** — 실제로 바다(육지까지 8.5km)가 맞지만 **고쳐도 일변화가 안 살아난다**
  (2.05K → 1.96K). 고산은 좁은 곶이라 R030 격자에선 어느 셀도 바다가 지배적. **건드리지 말 것.**
- ★**일변화 압축은 지점 문제** — east 는 실측 진폭의 **94%** 를 재현한다(west 27% / south 31%).
- **KPX `*_da` 중복 버그 수정** — API 가 1~9시를 `'01'`/`'1'` 두 형식으로 발행(하루 66행).

## 현재 상태 — **현재 시점까지 따라잡음**

| | 상태 |
|---|---|
| `historical` | 08-24 23:00, `*_da` 결손 0 |
| `forecast_horizon` | **245 base 전부 완전** (`--verify` rc=0) |
| `est_horizon_jeju` | **245 base / 08-23** |
| `est_smp_horizon_jeju` | **245 base** |
| 화면 | AppTest 렌더 exception 0 |

서버는 offline. `forecast_kimr`(07-29)·`forecast_kimg`(08-11) 아카이브만 정지 상태다.

## 다음 할 일

1. **②-2 `collect_archive --backfill`** — 재학습용 아카이브. 서빙은 안 읽어 급하지 않으나
   **API 보존 80일(완전)/160일(상한)** 이 걸린다.
2. **`sanity_check` 임계값** — `temp_skin` 이 매 base 걸려 **매일 rc=1** 이 뜬다(경보 피로).
   지표온은 기온보다 원래 급변한다. 임계 분리 또는 서빙 미사용 변수 제외를 검토.
3. **화면 통보** — `src_solar_cloud='KIMR'` 배지가 없다(`pages/common.py`).
4. **git push** (사용자). 로컬 커밋이 리모트 `Initial commit` 위에 쌓여 있다.
5. **8월 스케일링 재판정** — `fit_solar_scale.py --check` 로 흐림 편향 **부호** 확인.
6. **2026-12**: NC `GRAUPEL` = 구 `TCOG` 값 일치 확인.

### 닫힌 길 (다시 열지 말 것)
- **시각별 편향 보정** (사용자 결정 — 성능이 아니라 방침).
- **met 소스 변수별 분리** — `src_met_<지점>` 이 지점당 출처 하나만 담아 **출처 표시가 거짓이 된다**.
- **west 좌표 이동** — 좌표는 실제로 바다지만 고쳐도 진폭이 안 살아난다.
- 일사 결합(누적 ACSWDNB) / CAPE·CINN·HPBL·MSLP 태양광 신호 / 소스 폴백을 보간보다 먼저 /
  SMP D+2 안정구간 블렌딩 / 시점별 일사 QM / 전면 KIMR 전환.

## 주의사항

- ★**모델이 먹는 형태로 재라.** 지점별 우열이 3지점 평균에서는 상쇄돼 사라진다(2026-08-24 실증).
- ★**입력 지표 개선이 출력 개선으로 자동 전달되지 않는다.** 입력을 손대면 서빙 A/B 로 출력까지.
- ★**자유도를 늘리면 과적합한다.** 후처리 파라미터는 **완전 홀드아웃**으로 재라.
- ★**두 소스를 비교할 땐 같은 행 집합에서.** 서로 다른 표본으로 재서 08-12 결론이 뒤집혔다.
- **`temp_skin` 은 서·남부가 얼어붙어 있다**(1h 변화 0.014/0.033 vs east 1.15) — 재학습 피처로 쓰지 말 것.
- **SMP D+2 는 lag168** — `smp_jeju_da` 에 하루만 구멍이 나도 그 +7일 D+2 가 빈다.
- **KPX 쿼터는 서비스별**이고 일일 제한이다 — 백필 전 진단 호출을 아낄 것.
- **`forecast_horizon` 의 옛 `cape`/`cinn`** 은 재학습에 쓰지 말 것(`src_met_proto` 로 거른다).
- **PatchTST 가중치 D+1~D+5 는 스케일러 공유 = 한 세트.**
