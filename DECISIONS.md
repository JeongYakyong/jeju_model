# DECISIONS

> 한 줄에 하나: 결정 내용 (이유). 최신이 아래.

## 2026-07-21

- land 관련 코드 전면 삭제 (제주 전용 저장소이고, `--region jeju` 경로에서 한 줄도 실행된 적 없음)
- 화면 계층을 `pages/` 로 분리 (`st.navigation` 이 Streamlit 자동탐색을 꺼서 헬퍼가 사이드바에 새지 않음)
- `_common` + `api_fetchers_jeju` 는 한 파일로 합치지 않음 (common/specific 이 아니라 KIMG/KIMR 병렬 어댑터)
- 미래 `*_da` 공급을 `build_historical` 로 이관하고 `build()` 폐지 (파이프라인이 `--no-forecast` 라 내일치 DA 가 한 번도 안 들어오고 있었음)
- 저장은 wide 유지, 전면 long 저장 기각 (행 57배 증가 대비 NULL 절감 6%, 모델이 wide 피처 행렬을 요구)
- `src_met_{west,east,south}` 마스크 신설 (`combine_first` 가 셀 단위라 wide 만으로는 출처를 알 수 없음)
- 주/백업 소스 교체 없음 (실측 MAE 로 현행 병합이 이미 각 변수의 우세 소스와 일치 — 일사·운량 KIMG, 풍속 KIMR)
- 소스 비교에 상관계수 대신 MAE 사용 (장마창은 변동이 없어 r 이 노이즈를 잼 — std 0.18°C 에서 r=−0.816 사례)
- KIMR 아카이브를 NC 단일 스택으로 자체완결 (일사를 받으려면 어차피 NC per-hf 를 도니까 met 을 얹으면 호출수 증가 0)
- GRIB 경로는 삭제하지 않고 `--met grib` 로 보존 (NC 장애 시 즉시 복귀 + 90일 이전 구간 TSKIN 확보)
- `forecast_horizon`(서빙 입력)의 일사·운량은 KIMG 유지 (모델이 NE57 분포로 학습 + 실측 MAE 도 KIMG 우세)
- `collect_weather_kim` 의 `collect_forecast` 흡수는 본 DB 병합 시점으로 연기 (지금 합치면 독립 경로 2개가 나란히 놓일 뿐)

## 2026-07-22

- `--force` 를 skip 무시가 아니라 **해당 base 행 교체**로 정의 (COALESCE upsert 가 NC 미제공 컬럼에 GRIB 잔재를 남겨 한 행에 두 소스가 섞임)
- `--force` 의 DELETE 는 수집 성공 후에만 실행 (실패하면 삭제 자체를 안 하므로 데이터 손실 경로가 없음)
- 아카이브에 `src_met_proto`(`NC`/`GRIB`) 태그 신설 (wide 저장이 수집 방식을 지움 — 테이블이 모델을 말하므로 남는 축은 프로토콜뿐)
- KIMG 테이블은 태그하지 않음 (수집 경로가 하나뿐이라 정보량 0)
- collectors 개명 시 "이름만 바꾸기" 기각, 개명 + 최소 분리 채택 (`api_fetchers_jeju` 에 성격이 다른 셋이 들어 있어 새 이름도 절반만 맞음)
- `api_fetchers_jeju` 를 `kma_kimr`/`kpx_asos`/`pivot` 셋으로 분리 (`collect_forecast` 가 이미 `kim`/`ci`/`kpx` 세 별칭으로 부르고 있었고 사용 함수가 정확히 그렇게 갈림)
- 파일을 가르는 축 = 출처가 아니라 예보냐 실측이냐 (ASOS 는 KMA 소스지만 관측이라 `kma_*` 가 아니라 `kpx_asos` 에 둠)
- `kma_kimg` 에서 KMA 공용 기반(키풀·세션·창 SSOT)을 떼지 않음 (KIMG core 가 그 위에 얹혀 있어 가르면 두 파일이 서로를 계속 부름)
- `selftest_pivot` 을 `postprocess` 에 합치지 않음 (postprocess 는 forecasting/ 도 import — 테스트가 서빙 의존성에 딸려 들어감. 검사 대상도 `pivot.py` 지 postprocess 가 아님)
- collectors 구조 정리는 여기서 완료 선언, `long_to_wide_v2` 통합은 본 DB 병합 시점으로 연기 (통일 피벗이 누적 diff 를 강수 하나만 알아서 ACSWDNB 까지 넣으려면 운영 경로까지 건드려야 함)
- ★**KIMR GRIB 경로는 도태 예정 — NC 안정성 확보 시 `kma_kimr_grib.py` 파일째 삭제** (사용자 3회 강조. 남은 이유는 유예 조건 둘뿐: NC 장애 폴백 + 90일 이전 `ACSWDNB`·`TSKIN` 결손 보전)
- `kma_kimr.py` → `kma_kimr_grib.py` 로 재개명, `_nc` 에서 "(아카이브 전용)" 라벨 제거 (평범한 이름이 죽을 쪽에 붙어 NC 가 곁가지로 보였음 — 이름이 도태 방향을 드러내야 함)
- KIMR 관련 새 코드·기능은 전부 `_nc` 에 쓴다, GRIB 에 얹지 않는다 (유지보수를 대등하게 하면 삭제가 미뤄짐)

## 2026-07-30

- 지난 세션 "일사·운량 KIMG 최적" 철회 (7월 장마창 12 base + 순시 컬럼 두 겹 함정. 161일·적산으로 재니 접전)
- 소스 선정 프레임: KIMG 를 근본으로 저장, 변수별로 KIMR 우수하면 KIMR 채택, KIMG 우수하면 재고 (사용자)
- 소스 비교 시 일사는 **적산(ACSWDNB) 컬럼**을 쓴다 (순시 SWDDIR2+SWDDIF2 는 시간적산 실측과 정의가 어긋나 +bias — 적산이 17% 낫다)
- 최종 소스 결정은 재학습 A/B 로 확정 (r 높은 KIMR 이 유리해 보이나 bias 보정 전제라 실측 학습 필요)
- KIMG 아카이브 백필 불필요 판정 (`forecast_horizon` 일사·운량이 곧 KIMG 소스이고 작년 12월부터 있음)
- 격리 아카이브(weather_kim.db) 폐지, forecast_kimr/kimg 를 메인 DB 로 흡수 (소스 비교 끝나 2026-07-17 격리 이유 소멸)
- 새 메인 DB 예보 구조 = 소스 분리 2테이블 유지 (변수별 KIMR/KIMG 선택 유연성 — 병합은 재학습이 결정)
- 모든 collector 가 메인 DB 에 기록 (`collect_archive.DB_PATH` = 메인). `input_data_jeju(temp).db` 는 임시일 뿐 격상 안 함
- KIMG 장기 이력은 temp 의 forecast_horizon 일사·운량에서 seed (KIMG met 은 미사용이라 seed 안 함, 백필 7.5h 생략)
- GRIB(forecast_horizon) 서빙 경로는 재학습까지 유지, 새 NC 서빙 확인 후 폐기 (Phase 3)

## 2026-07-31

- 운영 지평을 **D+1~D+5 로 통일** (demand 168h→120h, PatchTST D+6·D+7 폐기, serve_demand 가드 1~5) — 예보 수집이 --days 5 라 D+6/7 서빙 입력이 기후값 폴백뿐이었다
- 재학습 학습창 통일: train ≤2026-01 / val 2026-02~05 / test 2026-06~07
- 배포본은 **train+val+test 전체 재적합** (val 로 잡은 best_iteration 고정, 성능표는 검증본 기준)
- 학습·서빙 데이터 소스를 얼린 CSV 에서 **메인 DB** 로 전환 (서빙 CSV 의존은 BTM 용량 1개만 남음)
- serve_solarwind_lgbm 의 clearsky 평년은 `feat_meta.json` 의 train 을 읽어 **학습창을 자동 추종** (하드코딩 ≤2024 제거)
- **models/ 는 git 추적** — 배포가 서버 `git pull` 이라 가중치가 저장소에 있어야 한다 (CLAUDE.md 의 "모델은 gitignore 대상" 기술을 정정). Training/ 대용량 233MB 는 제외
- solar 채널은 **PatchTST 유지** (재학습 LGBM 이 근소 우위였으나 2026-06-08 채널 설계를 뒤집지 않음)
- **노이즈 주입 포기** — 예보 오차가 무작위가 아니라 regime 의존 체계 편향이라 후처리로 대응
- **일사 QM 은 실패로 확인** — 시점별 매핑은 조건부 편향(흐림 +0.41 / 맑음 −0.26, 방향 반대)을 못 잡는다. 단순·비대칭·조건부 세 변형 모두 흐림만 개선하고 맑음을 악화
- **태양광 일 스케일링 후처리 채택** (구 파이프라인에서 이식) — 하루 단위로 그날 성격을 판정해야 조건부 편향이 잡힌다
- 일 스케일링 판정지표 = **west·south 중 어두운 쪽(min)의 P60** — 지점 조합 × 분위수 전탐색 결과. 초창기 방식(평균 P75)은 후보 중 최하위
- 일 스케일링 mid=1.10·K=5.5 **고정**, floor 만 지평별 — 셋을 다 풀면 과적합(검증 MAE 0.1321→0.1379)
- SMP 는 재학습 보류, **D+2 지평 유지** (D+1 가격은 발표된 DA 그대로라 모델이 아니고, 실제 학습 모델은 음수경보와 D+2 잔차회귀 둘뿐)
- `fit_solar_qm.py` 삭제, `Training/` 학습 산출물 231MB·D6/D7 가중치 22MB 삭제 (코드 111개는 보존)
- ★"NC 전환"과 "일사 소스 전환"은 별개임을 실측 확인: met GRIB→NC 는 r=0.99992(포맷 차이, 예측 불변) / 일사 KIMG→KIMR 은 r=0.64~0.70(다른 모델, 진짜 A/B 대상)
- ★현 구조에서 "KIMR base 모델"은 성립하지 않음을 확인 — 모든 모델이 **실측 기상으로 학습**하므로 소스를 바꿔도 모델은 동일하다. 소스 전환으로 바뀌는 것은 서빙 입력과 보정(스케일·tcog) 재적합뿐
- 현재 모델 + KIMR 입력 실측(2026-03~06, 스케일 적용 공정비교): solar MAE 0.1342(KIMG) vs 0.1543~0.1597(KIMR) — **KIMR 이 15~19% 열세**, 흐림 bias +0.0025 → +0.07~0.10, 과대율 36.9% → 51.7~63.1%. 일사 순시는 맑음 bias 가 +0.036(과대)로 뒤집혀 위험
- → **전면 KIMR 전환 보류.** met 은 값이 같아(r=0.99992) GRIB→NC 는 코드 정리로만, 일사·운량은 현행 KIMG 유지가 낫다 (2026-07-30 "변수별 소스 선택" 프레임 유지)
- KIMR 입력용 스케일 재적합해도 KIMG 에 못 미침 (검증: 흐림 +0.0415 vs +0.0025, MAE 0.1464 vs 0.1342, 과대율 45.8% vs 36.9%). 충분히 누르려면 mid 3.2 가 필요한데 그러면 맑음 bias 가 -0.1433 으로 훼손된다
- met 계열(기온·풍속·풍향·습도·강수)은 GRIB↔NC r=0.998~0.9997 로 동일 → **wind LGBM·wind_qm.json 은 NC 전환에 영향 없음**. GRIB 폐기 진행 가능
- 운량은 KIMG↔KIMR r=0.47(전운량)·0.58(중하운량), KIMR 이 훨씬 맑게 본다(0.431 vs 0.713) — 일사와 함께 solar 과대예측의 원인
- → **결론: 전면 KIMR 전환 안 함.** met 은 NC 로(값 동일), 일사·운량은 KIMG 유지

## 2026-08-04

- ★**KIMR GRIB 경로 완전 폐기** — `kma_kimr_grib.py` 파일 삭제 + `kma_kimr_nc` 의 GRIB 잔재 + `collect_archive --met` 인자까지 제거 (유예 조건 둘이 모두 소멸: 서빙 입력까지 NC 로 넘어와 폴백 대상이 없고, TSKIN 은 서빙 미사용·ACSWDNB 는 사다리꼴 근사가 있다)
- 서빙 입력(`forecast_horizon`)의 met 을 GRIB → std NC 로 전환 (서빙 A/B 수요 상대차 0.002%, net_load 0.017% — 사실상 무손상)
- ★GRIB 쪽에 **데이터 결함**이 실증됨 — `cape`/`cinn` 의 9999 sentinel(전체의 57%/68%)과 2바이트 정수 랩어라운드(차이가 정확히 655.36 = 2¹⁶/100 의 배수). NC 는 둘 다 없다 → `forecast_horizon` 의 **옛 cape/cinn 은 재학습에 쓰면 안 된다**
- NC met 어댑터는 **출력 라벨을 구 GRIB 관례로 맞춘다**(`fetch_kimr_met_long`: TEMP=K, 반올림 없음) — 그래야 `pivot._SPEC_KIMR`·`selftest_pivot`·`forecast_horizon` 스키마가 전부 불변이고 전환 검증이 "값이 같은가" 하나로 단순해진다 (`_SPEC_KIMR` 을 고치는 쪽은 17항목 회귀망 기준선을 건드리게 되어 기각)
- 구 GRIB `TCOG`(싸락눈)는 NC 에서 이름이 **`GRAUPEL`** 이고 기존 변수 세트와 한 콜에 온다 (호출수 증가 0). 구 `TCOH`(우박)는 NC 에 없지만 실측 32,012행 × 3지점이 전부 0 이라 손실 없음
- NC per-hf 수집 스택을 `kma_kimr_nc.fetch_nc_long` **한 벌로 통합** — 운영·아카이브의 차이는 요청 변수 목록과 카테고리 파생 함수뿐 (2026-07-05 "NC per-hf 는 혼잡 밤 마비" 우려는 아카이브의 161일 백필 + 매일 cron 실적으로 뒤집힘 — 지점당 ~10s)
- **3h 결손 보간을 수집 단계로** — `to_grid` 를 `postprocess.fill_short_gaps` 로 올려 `collect_forecast`·`collect_archive` 공용화, limit=2(연속 2개=3h 결손을 정확히 덮음), 외삽 금지
- 보간은 반드시 `clip_ranges` **앞**이다 (clip_ranges 가 radiation/rainfall NaN 을 0 으로 채우므로 뒤에 두면 메울 결손이 이미 0 으로 위조된다)
- 보간 그리드는 `expected_timestamps`(=`collection_hf_range`)를 그대로 쓴다 — 1h/3h 해상도 전환을 이미 반영한 SSOT 라 그리드를 추측하지 않는다 (운영 창 --days 5 는 전 구간 1h)
- ★**SMP D+2 개선안(안정 구간 shrinkage / 앵커 블렌딩)은 기각** — 손실은 실재하나(사후 안정 구간에서 모델 4.33 vs lag24 1.46) **그 구간을 사전에 식별할 방법이 없다**. 사전 신호 5종(직전 7일 변동성·직전 24h 변동폭·|lag24−lag48|·시각·모델 자기확신) 전부 판별력 편차 0.03~0.27 (사후 신호는 2.26). λ·gating 스윕도 전체 최적이 현행(λ=1.0, θ=0)
- ★**태양광 일 스케일링 과적합 확인** — `solar_scale.json` 의 검증창 흐림 bias −0.0002 는 재현되지 않음(같은 창 재계산 +0.0268). 판정지표와 mid 를 **그 검증창 성능을 보고** 골라서 생긴 착시. 완전 홀드아웃(7/1~)에서는 MAE 를 11% 악화시키고 있었다
- 태양광 스케일링 **적합 목표를 편향 0 → 편향 50% 축소**로 (`BIAS_TARGET_FRAC=0.5`) — 편향의 크기 자체가 시간에 따라 줄어(적합창 +0.057 / 홀드아웃 +0.037) 0 을 겨냥하면 구조적으로 과보정된다. 홀드아웃 MAE 대가 7.7% → 2.6%
- 태양광 판정지표를 **`radiation_south` 단독 P60 고정** (사용자 지시: 한쪽으로 기준을 정할 것) — 지점 조합 4종이 홀드아웃에서 사실상 동일했고(MAE 변화 +0.0102~+0.0121) 조합 전탐색이 과적합 원인이었다. 서빙은 무수정(`columns` 단일이면 min/mean 동일)
- 태양광 `k` 는 **탐색하지 않고 3.5 고정** — 격자에 넣으면 적합창 노이즈를 쫓아 k=12(급경사)를 고르는데 분할 4개 전부에서 홀드아웃이 나빴다(MAE 대가 +0.0068~0.0089 vs k=3.5 의 +0.0041~0.0057). 구 코드가 k 를 고정한 판단은 옳았고 값만 5.5→3.5
- 태양광 파라미터를 **지평 공통 한 벌**로 (지평별 floor 는 D+1~D+5 가 같은 값으로 수렴해 정보 0). 자유 파라미터 5개 → **mid·floor 2개**
- 태양광 재적합 기간을 **전 구간(2025-12~2026-07, 223 base)**으로, `--months` 기본 5 → 9 (사용자 지시: 기간을 길게)
- 배포값 **mid 0.45 / k 3.5 / floor 0.10** — 홀드아웃 흐림 +0.0373→+0.0030, MAE 대가 +3.2%, 과대율 41.0%→36.2%. 발동 분포는 44.9% 가 사실상 무보정
- ★스케일링은 **낮추기만 하는 구조**라 예보가 과소예측하는 달엔 손해다 (2026-07 흐림 −0.049, 일곱 달 중 유일한 음수). 파라미터로 못 고치니 `--check` 로 편향 **부호**를 감시한다
- `fit_solar_scale.build_unscaled_predictions` 의 `_SSCALE = {}` 버그 수정 (`None` 으로) — 빈 dict 이 `cfg, params = ...` 언패킹을 터뜨리고 `except Exception: continue` 가 삼켜 **재적합이 조용히 0행으로 끝나고 있었다**. 관리자 화면 재적합 버튼도 그동안 무동작
- NC 신규 변수 중 **CAPE·CINN·HPBL·MSLP 는 태양광에 쓸 신호 없음**(|r| ≤ 0.055). 반면 **누적 총일사(`radiation_acswdnb`)는 유망**(흐림 잔차 r −0.31, 하루 판정지표로 실측 이용률 상관 0.713 > 현행 0.694) — `forecast_kimr` 에만 있어 재학습·서빙 입력 재설계 사안으로 이월. 산란비는 현행보다 열세로 기각
- ★**일사는 소스 교체가 아니라 결합이다** — 실측 ASOS 대조 결과 단독으로는 KIMG 가 최선(debias MAE south 0.582 vs 누적 0.608, KIMR 순시는 최대 5.81 로 실측 3.93 초과라 논외). 그러나 KIMG+누적 선형결합은 홀드아웃에서 일사 MAE **−7.0~−18.7%** (편향보정 단독 대비도 −5.5~−15.6%), 계수 안정(KIMG 0.39~0.47 / 누적 0.38~0.50)
- ★**그래서 재학습은 불필요** — 모델이 실측 기상으로 학습됐으므로 결합값을 실측에 회귀시켜 넣으면 모델 무수정으로 정확도가 오른다. 두 소스를 피처로 직접 넣으려면 예보 기반 학습이어야 하는데 ACSWDNB 완전 구간이 93일뿐이라 계절 커버리지 부족 — 지금 재학습은 부적절
- `input_data_jeju(temp).db`(57MB) 삭제 — 본 DB 의 완전한 부분집합임을 확인(5테이블 행수 + 표본 컬럼 합계 일치, 본 DB 가 더 최신이고 forecast_kimr/kimg 도 보유)
- `Training/**/no use/`(9MB)는 **남긴다** — `.gitignore` 가 제외하고 있어 커밋해도 이력에 안 남고, 이 폴더에서 태양광 일 스케일링을 되살린 전례가 있다. git 연결 후 처리
- git 은 사용자가 직접 연결하기로 — 현재 GitHub 저장소가 빈 상태로 `jeju_model/jeju_model/` 하위에 클론돼 있고 본체는 git 밖이다. 그 `.git` 을 루트로 올리는 쪽이 리모트·이력 유지에 깨끗하다

## 2026-08-04 (후속 — 재수집 후 재검증)

- ★**일사 결합(KIMG+누적) 배포하지 않는다** — 앞선 "7~19% 개선"은 착시였다. 2026-07-02~11 사고가 운량뿐 아니라 **일사도 망가뜨렸고**(실측 대비 MAE 1.0786 → 재수집 후 0.6432, 값이 평균 0.43 낮게 기록됨), 결합이 그 손상을 보정하고 있었다. 데이터를 고치니 KIMG 0.6021 vs 제약결합 0.6022 로 이득 소멸
- 서빙 전달률도 0 으로 실측 (스케일 끄고 A/B, 1,675행): 전체 MAE −0.31%, 흐림 +1.28% 악화, 지평별 방향 불일치
- ★**입력 지표 개선이 출력 개선으로 자동 전달되지 않는다** — 결합의 MAE 이득은 정보 추가가 아니라 **분산 축소**였다(무제약 최소제곱은 std 를 실측의 0.792배로 압축). 일사 지표는 좋아지지만 모델이 쓰는 진폭이 사라진다. 시점별 QM 실패·스케일 과보정과 같은 계열의 함정
- `--verify` sentinel 에 **`total_cloud_*` 추가** — temp 만 보던 탓에 2026-07-02~11 운량 사고를 10 base 동안 못 잡았다. 운량은 전 지평 KIMG 단독 공급이라 D+1~5 NULL 은 실패가 맞다
- `--verify` 기대 행수를 수집 창에서 계산 (구 하드코딩 144 는 D+7 시절 값), NULL 검사도 운영 지평으로 제한
- `REGIONS` 중복 정의 제거, 기본 창 7 → **5**(운영값)
- 2026-07-02~11 결손 base 10개 재수집 — 운량 56/134 → 120/120, 일사 MAE −40%. 이 구간을 쓴 스케일 적합은 재실행
- `Training/**/no use/` 는 이력에 한 번 커밋(aee9bab)한 뒤 삭제 — `.gitignore` 가 제외하고 있어 그냥 지우면 복구 불가였다
- git 을 프로젝트 루트로 연결 (중첩 클론 `jeju_model/jeju_model/.git` 을 루트로 이동, 리모트·Initial commit 이력 유지). push 는 사용자가 직접

## 2026-08-12

- ★**수집 사다리 채택** — ①KIMG → ②시간 보간(연속 2개) → ③KIMR 대체 → ④sentinel 정정·clip·건전성 검사. **②가 ③보다 먼저**인 게 핵심(정답 대조 실측: 보간 MAE 0.063~0.106 vs KIMR 대체 0.346~0.490)
- ★**KIMG 통째 실패 시 KIMR 로 일사·운량 대체** — "예측 없음"보다 "오차 있는 예측 + 출처 통보"가 낫다(사용자 판단). 실패 시뮬레이션에서 1,062셀 전부 복구, 일사 r 0.94 / 운량 r 0.40. 출처는 `src_solar_cloud` 컬럼에 기록
- **소스 폴백을 시간 보간보다 먼저 쓰지 않는다** — 짧은 결손에서 KIMR 대체는 MAE 5~7배 나쁘고 큰오차 43~59%. 두 모델이 다른 구름을 본다(전운량 r 0.47)
- ★**9999 는 이상치가 아니라 결측** — cape/cinn 의 9999 = "대류불안정 없음". 숫자로 두면 cape 평균이 2073.0(실제 256.6)으로 8배 왜곡된다. `drop_sentinels` 가 `clip_ranges` **앞**에서 NaN 정정
- **이상치 탐지는 "고치기"가 아니라 "드러내기"** (`sanity_check`) — 무엇이 이상인지는 사람이 판단해야 하고 잘못 고치면 더 나쁘다. 경고만 내고 rc=1 로 올린다
- **KIMG 수집 13 → 22변수** (한 콜에 전부 오므로 호출수 증가 0). `hpbl` 은 KIMR 에도 있다 — 2026-06-13 "KIMG 에 없음" 기록은 틀렸다. `temp_skin`·`hpbl` 은 두 모델 컬럼명을 맞춰 join 비교가 가능하게 함
- `lcld`/`mcld` 원시값도 저장 (기존엔 `MIDLOW_CLOUD` 로만 접혔다) — 층별 운량 구조를 보려면 raw 가 필요
- ★**NWP 는 일변화 진폭을 크게 눌러 재현한다** — 실측 기온 주야 진폭 3.36°C vs KIMG 1.28 / KIMR 0.84, 지표온·경계층고도는 거의 평평. 두 모델 공통(제주=작은 섬). `temp_skin`·`hpbl` 은 이래서 신호로 쓰기 어렵다
- ★**소스 교체보다 시각별 편향 보정이 지렛대가 크다** — 시각 보정만으로 KIMR 기온 MAE −13.5%(KIMG −2.7%), 보정 후엔 KIMR 이 KIMG 를 이긴다. "기온은 KIMG 우세"는 보정 가능한 편향이었다. 단 계절엔 불안정하니 서빙 A/B 필수
- **서빙 체인 소스 구조 변경은 보류** — KIMG met 백필(185 base)이 끝나 비교는 가능해졌으나, 시각 보정을 먼저 보고 정한다
- `--verify` 가 불완전 base 시 rc=1, 수집도 건전성 경고 시 rc=1 (스크립트·파이프라인이 판정 가능)
- **파이프라인 단계 구성(`PIPELINE_STEPS`)은 안 바꿨다** — 이번 세션 변경은 전부 각 단계 **내부** 동작
